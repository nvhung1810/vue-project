<!-- Function Refs -->

<!-- 
    Thay vì khóa chuỗi, thuộc tính ref cũng có thể được liên kết với một hàm,
    hàm này sẽ được gọi trên mỗi bản cập nhật thành phần và mang lại cho bạn sự linh hoạt hoàn toàn về nơi lưu trữ tham chiếu phần tử.
    Hàm nhận tham chiếu phần tử làm đối số đầu tiên:

    <input :ref="(el) => { /* assign el to a property or ref */ }">
-->